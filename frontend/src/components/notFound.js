import React from 'react';
import {Link} from 'react-router-dom';

function NotFound (props){
    return(
        <main>
            <h1>Sorry, that URL does not exist.</h1>
        </main>
    )
}

export default NotFound;